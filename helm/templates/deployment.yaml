---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mahesh
  labels:
    app: nodejs-springboot
    env: qa
spec:
  selector:
    matchLabels:
      app: nodejs-springboot
  replicas: 1
  template:
    metadata:
      name: mahesh-pod
      labels:
        app: nodejs-springboot
        env: qa
  
    spec:
      containers:
        - name: mahesh-cnt
          image: maheshkumar010/mahesh:3
          ports:
            - containerPort: 5430
          
          livenessProbe:
            httpGet:
              path: /
              port: 5430
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /
              port: 5430
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 5
